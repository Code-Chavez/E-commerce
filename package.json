{
  "name": "e-commerce-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "E-Commerce monorepo - client and server packages",
  "scripts": {
    "dev:client": "pnpm --filter client dev",
    "dev:server": "pnpm --filter server dev"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  }
}
